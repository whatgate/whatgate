# Normalize line endings: keep LF in the repo for all text; Go tooling and
# cross-platform builds expect LF. Prevents CRLF churn on Windows checkouts.
* text=auto eol=lf

# Explicitly treat Go sources as LF text.
*.go text eol=lf
*.mod text eol=lf
*.sum text eol=lf
